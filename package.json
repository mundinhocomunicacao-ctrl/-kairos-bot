{
  "name": "kairos-whatsapp-cloud",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "npm test && node server.js",
    "test": "node bridge-contract.test.js"
  },
  "engines": {
    "node": ">=20"
  }
}
